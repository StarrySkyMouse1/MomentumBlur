<Page x:Class="Mmod.App.Views.Pages.ComposePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="合成"
      AllowDrop="True"
      Drop="Page_Drop"
      DragOver="Page_DragOver">
    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Page.Resources>
    <Grid Margin="8,16,8,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="True">
            <ui:Button DockPanel.Dock="Right"
                       Content="打开成片目录"
                       Command="{Binding OpenOutputFolderCommand}"
                       VerticalAlignment="Top"
                       Margin="12,0,0,0" />
            <StackPanel>
                <TextBlock FontSize="22" FontWeight="SemiBold" Text="合成" />
                <TextBlock Margin="0,6,0,0"
                           Opacity="0.75"
                           Text="{Binding ModeSummary}" />
                <TextBlock Margin="0,4,0,0"
                           Text="{Binding StatusText}" />
            </StackPanel>
        </DockPanel>

        <Border Grid.Row="1"
                Margin="0,16,0,0"
                Padding="12"
                BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Visibility="{Binding IsTgaMode, Converter={StaticResource BoolToVis}}">
            <StackPanel>
                <TextBlock FontWeight="SemiBold" Text="游戏 TGA" />
                <TextBlock Margin="0,6,0,0" Text="{Binding TgaMetricsText}" />
                <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                    <ui:Button Content="开始监视"
                               Appearance="Primary"
                               Command="{Binding StartTgaCommand}"
                               Margin="0,0,8,0" />
                    <ui:Button Content="停止并收尾"
                               Command="{Binding StopTgaCommand}" />
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="1"
                Margin="0,16,0,0"
                Padding="12"
                BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Visibility="{Binding IsObsMode, Converter={StaticResource BoolToVis}}">
            <StackPanel>
                <TextBlock FontWeight="SemiBold" Text="OBS 批量" />
                <TextBlock Margin="0,6,0,0" Text="{Binding BatchSummary}" />
                <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                    <ui:Button Content="添加视频…"
                               Command="{Binding AddVideosCommand}"
                               Margin="0,0,8,0" />
                    <ui:Button Content="开始批量合成"
                               Appearance="Primary"
                               Command="{Binding StartObsBatchCommand}"
                               Margin="0,0,8,0" />
                    <ui:Button Content="取消"
                               Command="{Binding CancelObsBatchCommand}"
                               Margin="0,0,8,0" />
                    <ui:Button Content="清空队列"
                               Command="{Binding ClearBatchCommand}" />
                </StackPanel>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="2"
                  Margin="0,16,0,0"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  ItemsSource="{Binding BatchItems}"
                  Visibility="{Binding IsObsMode, Converter={StaticResource BoolToVis}}">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="选"
                                        Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"
                                        Width="40" />
                <DataGridTextColumn Header="文件"
                                    Binding="{Binding Name}"
                                    IsReadOnly="True"
                                    Width="*" />
                <DataGridTextColumn Header="状态"
                                    Binding="{Binding Status}"
                                    IsReadOnly="True"
                                    Width="2*" />
                <DataGridTextColumn Header="%"
                                    Binding="{Binding ProgressPercent, StringFormat={}{0:0}}"
                                    IsReadOnly="True"
                                    Width="50" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
