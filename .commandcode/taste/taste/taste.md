# Taste
- Communicates in Chinese and expects responses in Chinese. Confidence: 0.9
- Prefers committing and pushing code directly with a direct merge — explicitly does not want PRs ("提交并推送代码，不用提交pr直接合并"). Confidence: 0.9
- Prefers discussing and confirming a modification plan before any code changes — will mark phases as "discussion only, no code changes", then confirm, then implement. Confidence: 0.8
- Prefers sequential execution of all planned steps until a feature is fully implemented. Confidence: 0.7
- For video/recording capture, prefers capturing at the highest quality with no compression at capture time; compression and quality loss are deferred to post-production in the editing software, and large file sizes (e.g., ~1 GB/min) are acceptable. Confidence: 0.8
- Prefers removing superseded logic completely rather than keeping hidden fallback thresholds or parallel judgment paths (e.g., no hidden byte-based floor once a percentage contract exists; "unknown"/Unavailable must never be treated as success). Confidence: 0.85
- Prefers centralized production defaults in policy/configuration classes; consumers must not hardcode production defaults as magic numbers, and production code must never gain test-only switches or debug APIs — tests override with smaller/faster values instead. Confidence: 0.85
- Prefers deterministic fake-based tests over real I/O: logic tests must not touch real disks and must avoid Thread.Sleep-style long waits; fakes expose call counts and scripted snapshot sequences to prove behavior (e.g., throttling, recovery). Confidence: 0.85
- Prefers deterministic, time-injectable tests over sleep-based pacing: time-driven components should expose explicit monotonic timestamp overloads (default production path keeps Stopwatch) so window/rate tests run without any Thread.Sleep; where sleeps are unavoidable, scale counters by measured elapsed time rather than assuming sleep durations (e.g., 10ms sleeps actually ~15.6ms on Windows). Confidence: 0.9
- Prefers minimal, meaningful diffs: rewrites that produce no substantive change are reverted, and accidental whitespace/line-ending-only diffs are restored to match the baseline exactly rather than shipped as noise. Confidence: 0.7
 every field of one snapshot must come from a single logical state under one lock, and locks must never be held across disk, native, UI, database, or other long-running work — never assume there is exactly one reader. Confidence: 0.85
- Prefers session-bounded mutable state over global statics: dedup and session state live on the instance and start empty per session, so state never leaks or pollutes across sessions. Confidence: 0.8
- Prefers backward-compatible persistence evolution: new enum values are strictly append-only (never renumbered, preserving old DB integer semantics), new persisted fields are added via nullable, idempotent EnsureColumn-style migrations, and old databases/records must read back exactly as the default "none" state. Confidence: 0.9
- Prefers validated-only promotion: an artifact counts as real only after actual content validation (real media facts — frame count, duration, resolution — never mere file existence or nonzero length), an atomic commit, and durable persistence; any failure in that chain yields no validated record. Confidence: 0.9
- Prefers conservative crash recovery: after interruption, preserve only durably validated and persisted artifacts, conservatively delete unvalidated/unmatched intermediates, and never adopt crash-window files as formal outputs; the affected unit returns to a re-runnable state and re-records from its head. Confidence: 0.85
- Prefers closing crash windows in file–database protocols by writing a durable intent/pending state to the DB BEFORE the atomic file operation (validate → persist pending intent with target path + reason → atomic move → promote to validated), so every intermediate crash point is recoverable and recovery resets any pending record back to none. Confidence: 0.9
- Prefers optimistic state guards on critical persistence transitions (e.g., `WHERE id AND state=$pending`) and verifying that UPDATEs affect exactly one row — 0 or multiple rows must throw an explicit persistence exception rather than silently proceeding or logging success. Confidence: 0.9
- Prefers stage/state names that describe what actually happened without overclaiming (e.g., "ControlledStopFinalized" rather than "PartialValidated" while validation hasn't run yet); a validated fact is only expressed by the dedicated validated state plus its persisted fields, and enum integer values stay stable for compatibility. Confidence: 0.85
- Prefers crash-matrix tests that enumerate each failure point of a persistence protocol (pending+temp, pending+partial, terminal+pending, validated+partial, unrelated files) and assert recovery converges correctly — pending cleaned and reset to none, validated kept, unrelated user files untouched. Confidence: 0.85
